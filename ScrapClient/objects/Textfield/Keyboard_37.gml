/// @description Insert description here
// You can write your code in this editor
if (keyboard_check_pressed(vk_left)||alarm[0]<=0)
{
if !keyboard_check(vk_shift) 
{
if pos2==pos&&pos>0 pos-=1;
else pos=min(pos,pos2);
pos2=pos;
	
}
else if pos>0 pos-=1;
alarm[0]=2;
}


