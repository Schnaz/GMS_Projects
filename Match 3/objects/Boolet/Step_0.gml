/// @description Insert description here
// You can write your code in this editor
if !alarm[0]
{
alarm[0]=16/mv; 
x+=dcos(ang)*16; 
y+=dsin(ang)*16;
if ff mask_index=mask_linked;
}
else mask_index=mask_discrete;

image_angle=-ang;
xpos+=mv*dcos(ang);
ypos+=mv*dsin(ang);