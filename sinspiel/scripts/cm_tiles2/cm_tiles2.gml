function cm_tiles2(xn, yn, zn, sz, a, sprite){
//cm_tiles(tiles per side, size, sprite)

var txu = sprite_get_uvs(sprite, 0);

	var iy = 0
	repeat(xn) {
		
		var ix = 0
		repeat(yn) {
			//tile(vb, s, ix, iy, xup, yup, zup)
			//cm_tile3(xx, yy, zz, vb, s, ix, iy, txu, xup, yup, zup)
		
			ix++
		}
		iy++
	}
}